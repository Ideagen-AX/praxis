dist/
node_modules/
*.tgz
.DS_Store

# The reference site, built by build-site.py from site/ and dist/. Generated for
# the same reason dist/ is: src/ and site/content/ are the sources, and a second
# committed copy is a second thing to keep in step.
_site/
__pycache__/
